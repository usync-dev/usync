{
  "name": "usync",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for the usync project.",
  "keywords": [],
  "license": "ISC",
  "author": "",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/usync-dev/usync.git"
  },
  "type": "module",
  "scripts": {
    "build": "pnpm run test && pnpm run clean && pnpm run build:js && pnpm run build:types",
    "clean": "pnpm run -r clean",
    "test": "pnpm run -r test",
    "build:types": "pnpm run -r build:types",
    "build:js": "pnpm run -r build:js",
    "format": "oxfmt"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.2",
    "@types/node": "^25.9.6",
    "del": "^8.0.1",
    "del-cli": "^7.0.0",
    "happy-dom": "^20.14.3",
    "linkedom": "^0.18.13",
    "oxfmt": "^0.67.0",
    "oxlint": "^1.82.0",
    "read-package-up": "^12.0.0",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vite": "^8.3.0",
    "vitest": "^5.0.0"
  },
  "engines": {
    "node": ">=24"
  }
}
